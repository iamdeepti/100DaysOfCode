#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
#define pii pair<int,int>
#define vi vector<int>
#define mp make_pair
#define pb push_back
#define f first
#define s second
int main()
{
    int n,q;
    cin>>n>>q;
    vi a(n);
    for(int i=0;i<n;i++)
        cin>>a[i];
    vi b(n);
    for(int i=0;i<q;i++)
    {
        cin>>b[i];
        if(b[i]<0)
        {
            a[i]
        }
    }
    return 0;
}